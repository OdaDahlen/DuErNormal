/*

package com.example.oda.duernormal;


import android.app.Activity;
import android.os.Bundle;

//import android.support.design.widget.FloatingActionButton;
import android.text.TextUtils;
import android.util.Log;
import android.view.View;
import android.widget.EditText;
import android.widget.Toast;

import com.google.firebase.database.DataSnapshot;
import com.google.firebase.database.DatabaseError;
import com.google.firebase.database.DatabaseReference;
import com.google.firebase.database.FirebaseDatabase;
import com.google.firebase.database.ValueEventListener;
import com.google.firebase.database.DatabaseReference;
import com.google.firebase.database.FirebaseDatabase;

public class FiredatabaseHandler extends FirebaseDatabase {
    //private DatabaseReference mDatabase;
    public FiredatabaseHandler(DatabaseReference mDatabase) {
        mDatabase = new FiredatabaseHandler().getInstance().getReference();
        //Actually - this makes no sense!!
        // The constructuor should be used if we actually need to
    }

};
 */